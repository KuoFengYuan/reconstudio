# runtime / scratch (job state, console + pipeline logs, points.ply cache, tmp)
data/
*.tmp

# per-machine config (copy from *.example)
local.env
backends.json

# python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# editor / OS
.vscode/
.idea/
.DS_Store

# vendored build artifact — regenerate with tools/build_supersplat.sh
static/supersplat/
