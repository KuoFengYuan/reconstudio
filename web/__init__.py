"""Web layer: FastAPI routers + shared template/render helpers + request-side
services. The app factory in app.py wires these together; the heavy lifting
stays in the torch-free `pipeline/` package.
"""
