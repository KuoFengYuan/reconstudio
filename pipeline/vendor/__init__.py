"""Vendored third-party utilities used by the large-scene COLMAP path.

Kept in a sub-package so their provenance + licenses stay obvious and the rest of
the pipeline imports them by a stable path (`from .vendor.read_write_model import …`).
"""
